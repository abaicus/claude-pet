{
  "name": "claude-pet",
  "version": "0.1.0",
  "description": "A desktop tamagotchi that feeds on Claude Code activity.",
  "main": "src/main.js",
  "private": true,
  "scripts": {
    "start": "electron .",
    "test": "node --test 'test/**/*.test.js'"
  },
  "devDependencies": {
    "electron": "^33.0.0"
  }
}
